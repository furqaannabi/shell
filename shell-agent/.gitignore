node_modules/
dist/
*.tsbuildinfo
.env
# User plugins are local-only; README.md is checked in as an example.
plugins/*
!plugins/README.md
!plugins/sample.mjs
# mcp.json is checked in with the walrus demo server pre-configured.
# Users can extend it with private servers locally.
